---
# Valeurs par défaut — rôle hermes
# Variables liées aux secrets et à l'environnement

hermes_user: "{{ jarod_user }}"
hermes_home: "{{ jarod_home }}"
hermes_env_file: "{{ jarod_home }}/.env"
hermes_env_mode: "0600"
